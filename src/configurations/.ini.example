# ============================================================================
# QBitra Backend Configuration Example
# ============================================================================
# Copy this file to dev.ini, prod.ini, stage.ini, or test.ini based on APP_ENV
# Location: src/configurations/
# 
# This file contains configuration for development environment
# For production, create prod.ini with appropriate values

# ============================================================================
# Test Section (Required)
# ============================================================================
# This section is used for configuration validation
# DO NOT CHANGE THIS VALUE - it's used for testing
[Test]
value = ThisKeyIsForConfigTest

# ============================================================================
# Database Configuration
# ============================================================================
[Database]
# Database type: sqlite, postgresql, mysql
db_type = sqlite
db_path = ./qbitra.db
db_name = qbitra
db_host = localhost
db_port = 5432
db_user = postgres
db_password = 
seed_data_path = ./seeds/

# ============================================================================
# Redis Configuration
# ============================================================================
[Redis]
# Redis connection settings for caching and rate limiting
host = localhost
port = 6379
db = 0
password = 
max_connections = 50
socket_timeout = 5
socket_connect_timeout = 5
decode_responses = True

# ============================================================================
# MailTrap Configuration
# ============================================================================
[Mailtrap]
# MailTrap email service configuration
# Note: MAILTRAP_API_KEY must be set in .env file
sender_email = info@qbitra.io
sender_name = QBitra
# Template UUIDs (optional - for template-based emails)
welcome_template_id = 
verification_template_id = 
password_reset_template_id = 
notification_template_id = 

# ============================================================================
# Prometheus Configuration
# ============================================================================
[Prometheus]
# Prometheus metrics configuration
namespace = qbitra
job_name = qbitra_service
metrics_port = 9090
# Push gateway URL (optional - for pushing metrics)
# push_gateway_url = http://localhost:9091