# ============================================================================
# QBitra Backend Environment Variables
# ============================================================================
# Copy this file to .env and fill in your actual values
# Location: src/.env (same directory as this file)
# 
# SECURITY WARNING: Never commit .env file to version control!
# All sensitive values should be kept secret.

# ============================================================================
# Application Environment (Required)
# ============================================================================
# Application environment: dev, prod, stage, test, local
APP_ENV=dev

# ============================================================================
# Environment Test Key (Required)
# ============================================================================
# This key is used to validate .env file loading
# DO NOT CHANGE THIS VALUE - it's used for testing
TestKey=ThisKeyIsForEnvTest

# ============================================================================
# Security Configuration (Required)
# ============================================================================
# JWT Secret Key - Used for signing and verifying JWT tokens
# Generate a secure random key: python -c "import secrets; print(secrets.token_urlsafe(64))"
# Minimum length: 32 characters (recommended: 64+)
JWT_SECRET_KEY=your_jwt_secret_key_here_minimum_32_characters_long

# Encryption Key - Used for encrypting sensitive data (Fernet symmetric encryption)
# Format: Base64-encoded 32-byte key OR 64-character hex string
# Generate: python -c "from cryptography.fernet import Fernet; print(Fernet.generate_key().decode())"
# OR: python -c "import secrets; print(secrets.token_hex(32))"
# If not set, a temporary key will be generated (NOT RECOMMENDED for production)
ENCRYPTION_KEY=your_encryption_key_here_base64_or_hex_format

# ============================================================================
# MailTrap Configuration (Required)
# ============================================================================
# MailTrap API key for sending emails
# Get your API key from: https://mailtrap.io/
MAILTRAP_API_KEY=your_mailtrap_api_key_here

# ============================================================================
# Database Configuration (Optional - can be set in .ini file)
# ============================================================================
# Uncomment and configure if you want to use environment variables for database
# DB_TYPE=sqlite
# DB_NAME=qbitra
# DB_HOST=localhost
# DB_PORT=5432
# DB_USER=postgres
# DB_PASSWORD=your_password_here

# ============================================================================
# Additional Environment Variables
# ============================================================================
# Add any other environment-specific variables here
