"""
User Roles route schemas.

Request and Response models for user roles endpoints.
"""
from typing import Optional
from pydantic import BaseModel, Field


# ============================================================================
# User Roles endpoints are read-only, no request schemas needed
# ============================================================================

# Response is dict from service, no specific schema needed

